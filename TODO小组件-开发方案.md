# TODO 桌面小组件 · 开发方案

> 生成日期：2026-08-31 ｜ 工作目录：`C:\Users\Administrator\Desktop\小组件`

## 一、项目概述

一个**双击即开、零依赖**的桌面 To Do 小组件（单文件 HTML），用于日常任务管理。参考成熟产品（TickTick、Microsoft To Do、Todoist）的核心交互，聚焦「文字 + 图片混合任务」这一差异化场景。

| 属性 | 内容 |
|---|---|
| 交付物 | `todo-widget.html`（单文件，内联 CSS/JS，无外部依赖） |
| 运行方式 | 双击用浏览器打开（Chrome / Edge），无需安装 |
| 数据存储 | 浏览器 localStorage 自动保存 + JSON 导出/导入备份 |
| 设计语言 | 深色顶栏 + 白色卡片 + 浅灰背景 + 克制蓝色点缀（与白板原型一致） |

## 二、需求确认记录（grill-me 决策表）

| # | 决策点 | 结论 |
|---|---|---|
| 1 | 技术形态 | **HTML 单文件**：双击即开，拖拽/图片/粘贴体验最佳，后期可打包桌面应用 |
| 2 | 完成删除交互 | **两段式防误删**：单击 = 标记完成（变灰划线）→ 完成态再单击 = 删除 |
| 3 | 图片导入方式 | **三种全支持**：文件选择对话框 / 拖拽图片到窗口 / 剪贴板粘贴（Ctrl+V） |
| 4 | 数据存储 | **自动保存 + JSON 备份**：实时落库，可导出/导入迁移 |
| 5 | 完成布局 | **原地划线**：完成的任务位置不变，变灰划线，再单击删除 |
| 6 | 增强功能 | **全选**：多列表分组、优先级标记、截止日期、搜索过滤 |
| 7 | 界面形态 | **桌面小组件样式**：紧凑卡片 + 圆角 + 蓝色点缀 |

## 三、功能规格

### 3.1 核心功能

| 功能 | 说明 | 交互细节 |
|---|---|---|
| 文字任务 | 输入框键入，回车添加 | 空内容忽略；支持长文本自动换行 |
| 图片任务 | 导入图片作为任务卡片 | 自动压缩（最长边 900px，JPEG 0.72）后入库，控制存储体积 |
| 图片导入 | 三种途径 | ① 工具栏「图片」按钮文件选择 ② 拖图片文件到窗口 ③ 截图后 Ctrl+V 粘贴 |
| 拖动排序 | 整卡拖拽重排 | 原生 HTML5 DnD，拖拽时显示插入位置高亮线，松手落位 |
| 单击完成 | 标记完成 | 卡片变灰 + 文字划线 + 左侧圆点变勾 |
| 完成再单击 | 彻底删除 | 弹出 5 秒「撤销」提示，防误删（参考 TickTick 做法） |
| 图片预览 | 点击缩略图放大 | 灯箱（lightbox）查看大图，点遮罩关闭 |

### 3.2 增强功能

| 功能 | 说明 |
|---|---|
| 多列表分组 | 顶部标签：工作 / 生活 / 购物 / + 新建；任务归属当前列表 |
| 优先级标记 | 高（红）/ 中（橙）/ 低（蓝）/ 无（灰），左侧圆点标识，点击循环切换 |
| 截止日期 | 任务卡片内置日期选择，逾期显示红色「已逾期」标记 |
| 搜索过滤 | 关键字实时搜索（匹配文字）；视图过滤：全部 / 未完成 / 已完成 / 今天 / 逾期 |
| 数据备份 | 工具栏「导出」下载 JSON，「导入」恢复；底部显示存储占用 |

## 四、技术方案

### 4.1 架构

```
todo-widget.html（单文件）
├── <style>   ── 设计令牌（CSS 变量）+ 组件样式
├── <body>    ── 顶栏 / 列表标签栏 / 输入区 / 任务列表 / 底部状态栏 / 灯箱
└── <script>  ── 数据层 + 渲染层 + 交互层
```

### 4.2 数据模型（localStorage `todoWidgetData_v1`）

```json
{
  "version": 1,
  "lists": [
    {
      "id": "l1",
      "name": "工作",
      "tasks": [
        {
          "id": "t1700000000000",
          "type": "text | image",
          "text": "写周报",              // 文字任务的内容；图片任务可加备注
          "image": "data:image/jpeg;base64,...",  // 仅图片任务
          "priority": "high | medium | low | none",
          "due": "2026-09-01",          // 可选
          "done": false,
          "createdAt": 1700000000000
        }
      ]
    }
  ]
}
```

### 4.3 关键技术点

| 技术点 | 方案 |
|---|---|
| 图片压缩 | Canvas 重绘：最长边缩到 900px，`toDataURL('image/jpeg', 0.72)`，单张约 50~150KB |
| 存储容量 | localStorage 上限约 5MB，压缩后约可存 30~100 张图；底部显示占用率，满时提示导出清理 |
| 粘贴导入 | 监听 `paste` 事件，读取 `clipboardData.items` 中 image 类型 |
| 拖拽排序 | `dragstart` 记录任务 id → `dragover` 计算插入位置（与卡片中线比较）→ `drop` 重排数组 |
| 图片拖拽冲突 | 卡片内 `<img>` 设 `draggable="false"` 并阻止冒泡，避免与整卡拖拽冲突 |
| 防误删 | 删除时置入 `pendingDeletes` 暂存区，5 秒后真正清除；期间显示撤销按钮 |
| 渲染 | 数据驱动：`render()` 全量重建列表，交互只改数据再重渲染，逻辑简单可靠 |

## 五、界面设计

```
┌────────────────────────────────┐
│ ▦ TODO 小组件       导出  导入  │ ← 深色顶栏
├────────────────────────────────┤
│ 工作 · 生活 · 购物 · ＋新建      │ ← 列表标签（激活态蓝色）
│ 🔍 搜索…  [全部|未完成|已完成|今天|逾期] │ ← 搜索与过滤
├────────────────────────────────┤
│ 输入文字，回车添加…      [🖼 图片]│ ← 输入区
│ ┌────────────────────────────┐ │
│ │ ● 写周报        高  📅 已逾期│ │ ← 任务卡片
│ │                            │ │
│ │ ▤ [图片缩略图]  中  📅 9/1  │ │ ← 图片任务
│ └────────────────────────────┘ │
│ 共 3 项 · 存储 12%  [清空已完成] │ ← 底部状态栏
└────────────────────────────────┘
```

配色：顶栏 `#1e2430` ｜ 背景 `#f5f6f8` ｜ 卡片 `#ffffff` ｜ 主色 `#3b82f6` ｜ 优先级 红`#ef4444` / 橙`#f97316` / 蓝`#3b82f6` / 灰`#9ca3af`。

## 六、风险与限制

| 风险 | 影响 | 应对 |
|---|---|---|
| localStorage 容量上限 | 图片任务过多时保存失败 | 图片压缩 + 占用率预警 + 导出备份 + 清空已完成 |
| 浏览器私有模式 | 数据不持久 | 文档注明使用普通模式 |
| 换浏览器/设备 | 数据不互通 | 提供 JSON 导出/导入迁移 |
| 大图内存占用 | 卡片渲染卡顿 | 渲染缩略图（CSS 定高），仅灯箱加载原图 |

## 七、验收清单

- [x] 文字任务添加 / 编辑 / 完成 / 删除全流程可用
- [x] 图片任务：三种导入方式均可添加，缩略图正常，点击可预览
- [x] 整卡拖拽排序，松手落位正确，刷新后顺序保持
- [x] 单击完成 → 变灰划线；再单击 → 删除且 5 秒内可撤销
- [x] 多列表切换、新建列表，数据互不串扰
- [x] 优先级循环切换、截止日期设置、逾期标红
- [x] 搜索与四种过滤视图正确
- [x] 刷新页面数据不丢失；导出 JSON 可重新导入

## 八、桌面化部署（钉桌面 + 开机自启）

> 2026-08-31 追加：让小组件成为真正的桌面应用——桌面图标双击即开、独立窗口、开机自动启动。

### 8.1 架构

```
┌─ 桌面快捷方式「TODO小组件.lnk」（开机启动项同款）───────┐
│ 双击 → pythonw.exe serve.py                            │
│           │                                           │
│           ├─ ① 本地静态服务 http://127.0.0.1:8765     │
│           │      （首次启动才起服务；已在运行则跳过）    │
│           └─ ② Edge --app 模式独立窗口（无浏览器界面） │
└──────────────────────────────────────────────────────┘
```

- **数据可靠性**：改用 `http://127.0.0.1:8765` 源存储 localStorage，彻底避开 file:// 协议在部分浏览器下的存储差异
- **独立窗口**：Edge/Chrome 的 `--app=` 应用模式，无地址栏/标签页，像原生应用
- **常驻服务**：pythonw 无控制台后台运行，窗口关闭后服务仍在，再次双击秒开

### 8.2 交付文件

| 文件 | 用途 |
|---|---|
| `serve.py` | 启动器：本地服务 + 打开独立窗口 + `--install` 一键安装 |
| `安装桌面小组件.bat` | 一键安装脚本（运行一次，创建桌面图标 + 开机启动项） |
| 桌面「TODO小组件」图标 | 快捷方式，双击启动 |
| 开机启动项 | `启动文件夹/TODO小组件.lnk`，登录后自动弹出 |

### 8.3 使用说明

1. **首次安装**：双击 `安装桌面小组件.bat`，运行完成后桌面出现「TODO小组件」图标，开机启动项自动就位
2. **日常使用**：双击桌面图标 → 独立窗口弹出，操作与网页版完全一致
3. **开机自启**：已自动安装到系统启动文件夹，登录 Windows 后自动弹出窗口
4. **二次双击**：服务已在运行时，只会再开一个窗口，不重复占用资源
5. **卸载**：删除桌面图标 + `启动文件夹/TODO小组件.lnk`，再删除 `serve.py` 与 `todo-widget.html` 即可（服务随开机启动项消失）

### 8.4 常见问题

| 问题 | 处理 |
|---|---|
| 端口 8765 被占用 | serve.py 自动检测，已在运行则直接开窗口；若被其他程序占用，改动 `PORT` 常量后重装 |
| 找不到 Edge | 自动回退 Chrome；都没有则用系统默认浏览器打开 |
| 想隐藏窗口 | 关闭窗口即可，服务继续后台运行，随时可再开 |
| 开机自启不生效 | 检查 `启动文件夹/TODO小组件.lnk` 是否存在；确认 pythonw.exe 路径未变 |

### 8.5 已验证

- [x] `--install` 创建桌面 + 开机启动项快捷方式成功
- [x] 双击快捷方式 → 服务器 200 + Edge 独立窗口弹出（标题「TODO 小组件」）
- [x] pythonw 常驻后台，二次双击不产生多余常驻进程
- [x] 开机启动项快捷方式存在于系统启动文件夹

## 九、托盘化升级（不占任务栏 + 永远在线）

> 2026-08-31 追加：原方案窗口会占任务栏一个按钮，容易被误关。改为系统托盘常驻——窗口工具窗口化（不显示在任务栏），右下角托盘图标统一管理。

### 9.1 变更要点

| 维度 | 原方案 | 托盘版 |
|---|---|---|
| 任务栏 | 显示一个 Edge 窗口按钮（易误关） | **完全不占任务栏**（窗口设了 `WS_EX_TOOLWINDOW` 样式） |
| 窗口唤起 | 双击桌面图标 | 双击桌面图标 / 双击托盘图标（均可） |
| 窗口控制 | 每次双击桌面图标都开新窗口 | 主实例常驻，新双击只显示已有窗口（HTTP `/__act__` 激活） |
| 退出 | 杀 `pythonw.exe` 进程 | 右键托盘 → 退出 |
| 入口图标 | Edge 自带图标 | 蓝色对勾小图标（Python PIL 绘制） |

### 9.2 托盘交互

- **双击托盘图标**（蓝色对勾）：显示 / 隐藏小组件窗口
- **右键托盘图标**：
  - 显示 / 隐藏小组件
  - 退出（彻底退出，含后台服务）
- **双击桌面图标**：等同「显示窗口」（主实例常驻时不会重复启动）

### 9.3 Windows 11 托盘固定（首次配置）

Windows 11 默认把**新创建**的托盘图标折叠到任务栏右侧的 `^` 溢出区，首次使用建议固定到主区：

1. 启动小组件后，点击任务栏右下角 `^` 箭头
2. 在弹出列表中找到蓝色对勾的「TODO 小组件」
3. 右键 → 选择「编辑」或「显示在任务栏上」
4. 图标即可固定在主托盘区（与微信、输入法等并列）

> 固定后无需再配置，每次开机自启即在主托盘区显示。

### 9.4 新增依赖

| 包 | 用途 |
|---|---|
| `pystray` | 跨平台系统托盘库（Windows 后端基于 `Shell_NotifyIcon`） |
| `pillow` | 生成托盘图标（蓝色对勾） |

安装命令（一次性）：
```
C:\Users\Administrator\.workbuddy\binaries\python\versions\3.13.12\python.exe -m pip install pystray pillow
```

### 9.5 已验证

- [x] 窗口样式 `WS_EX_TOOLWINDOW` 设置成功（`GetWindowLongPtrW` 读取确认 `TOOLWINDOW=True, APPWINDOW=False`）
- [x] pythonw 后台常驻（pystray 消息循环在主线程）
- [x] 隐藏后通过 `/__act__` 重新显示成功
- [x] 二次启动走 `/__act__` 激活后立即退出（不残留进程）
- [x] 托盘图标已注册（`Shell_NotifyIcon NIM_ADD` 成功，pystray 进程无异常）

## 十、图片无损存储（原图清晰度）

> 2026-08-31 追加：原方案为节省 5MB localStorage 容量，对图片做"最长边 900px + JPEG 0.72"压缩，导致导入截图/照片明显变糊。本次改造：图片改用 IndexedDB 原图无损存储，localStorage 只保留任务元数据。

### 10.1 变更前后对比

| 维度 | 原方案 | 新方案 |
|---|---|---|
| 存储后端 | `localStorage`（5MB 上限） | **`IndexedDB`**（数百 MB，按磁盘可用空间） |
| 压缩 | 最长边 900px + JPEG 0.72 | **原图无损**，格式不变 |
| 任务数据 | 内联 `image: dataURL`（数十~百 KB） | 只存 `imageId: 'img_xxx'` 引用（几十字节） |
| 灯箱清晰度 | 受压缩损失，文字/线条模糊 | **100% 原图锐利** |
| 旧数据兼容 | —— | 旧 `image` 字段继续显示，新 IDB 引用走异步加载 |

### 10.2 实现要点

- **图片导入**：`FileReader.readAsDataURL` 原样读取 → `idbPut(imageId, dataURL)` → 任务记录 `imageId` 引用
- **渲染**：同步先渲染占位（`.thumb.placeholder` 浅灰底）→ 异步 `idbGet` → 填入 `img.src` 移除占位
- **存储占用**：底部进度条仍显示 localStorage 占用（图片不在内），元数据一般 < 1%
- **删除清理**：
  - 任务删除：5 秒撤销窗口期内不动 IDB；过期后 `idbDel`
  - 清空已完成 / 删除列表：立即 `idbDel` 全部相关图片
- **导出备份**：JSON 嵌入 `images: {imageId: dataURL}` 完整打包原图，换机器导入后图片完整恢复
- **降级路径**：`file://` 直接打开时 IDB 不可用 → 退回压缩模式（1600px + JPEG 0.92 / PNG 保 PNG），并提示用户改用桌面小组件模式

### 10.3 已验证

- [x] 注入 1600×1200 PNG 测试图 → IDB 存 421930 字符（约 316KB 原图，格式 PNG 无损）
- [x] 灯箱全尺寸渲染：1px 细线条 + 18px 文字完全锐利（之前压缩版会糊）
- [x] 页面刷新后 IDB 持久化：原图完整保留，localStorage 仅 273 字节元数据
- [x] 旧任务内联图片（`t.image` 字段）继续显示，向后兼容
